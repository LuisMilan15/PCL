// Função para converter PCL para formato visualizável
const convertPCL = async (file) => {
  // Implementação da conversão PCL
  return new Promise((resolve) => {
    // Adicione aqui a lógica de conversão PCL
    resolve(file);
  });
};

export default convertPCL;